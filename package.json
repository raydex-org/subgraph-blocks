{
  "name": "subgraph-blocks",
  "version": "1.0.0",
  "repository": "https://github.com/raydex-org/subgraph-blocks",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "create-local": "graph create raydex/pulsechain-blocks --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy raydex/pulsechain-blocks --debug --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "watch-local": "graph deploy raydex/pulsechain-blocks --watch --debug --node http://127.0.0.1:8020/ --ipfs http://localhost:5001"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.2.2",
    "eslint-config-prettier": "^6.1.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.2"
  }
}
